class MenuBody {
  public readonly title: string
  public readonly menuId: string

  constructor(title: string, menuId: string) {
    this.title = title
    this.menuId = menuId
  }
}

export default MenuBody
